export const metadata = {
  title: "Hunting Calendar & Application Deadlines | Gray Bear Hunting Directory",
  description:
    "State hunting application deadlines, season dates, outdoor expos, safety courses, and community events across all 50 US states.",
};

export default function CalendarLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
